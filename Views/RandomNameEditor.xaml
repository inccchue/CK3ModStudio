<UserControl x:Class="WpfPrismFrameworkTemplate.Views.RandomNameEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="NameTextBox" 
                 Style="{StaticResource TextBoxExtend}"
                 hc:TitleElement.Title="성명"
hc:TitleElement.TitlePlacement="Left"
                 Margin="5"
                 Grid.Column="0"
                 Text="{Binding Value.Name, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <Button Grid.Column="1" Style="{StaticResource ButtonBaseStyle}" Background="#0000" BorderThickness="0" 
        ToolTip="이름 무작위 생성"  Height="35"
                Click="RandomButton_Click">
            <Button.Content>
                <Image Source="/Images/Random.png" />
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
