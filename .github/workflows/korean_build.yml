name: Build CK3ModStudio Safe Korean

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Safely Translate All UI, Tooltips, Menus and Dialogs
        shell: pwsh
        run: |
          $utf8WithBom = New-Object System.Text.UTF8Encoding($true)

          # 전수 번역 매핑 사전
          $dict = [ordered]@{
            # [1. 통계 열거형 식별자 (CS1003 문법 에러 방지용)]
            '各家族平均寿命'           = '가문별평균수명'
            '各文化人数'               = '문화별인원수'
            '各宗教人数'               = '종교별인원수'
            '性别比例'                 = '성별비율'
            '各家族人数'               = '가문별인원수'
            '各집안人数'               = '가문별인원수'

            # [2. 생애 이벤트 종류 (인물 편집 필수)]
            '出生'                     = '출생'
            '死亡'                     = '사망'
            '结婚'                     = '혼인'
            '婚姻'                     = '혼인'
            '生子'                     = '자녀 출산'
            '获得领地'                 = '영지 획득'
            '失去领地'                 = '영지 상실'
            '获得头衔'                 = '작위 획득'
            '失去头衔'                 = '작위 상실'
            '事件类型'                 = '이벤트 유형'
            '事件日期'                 = '이벤트 날짜'
            '日期'                     = '날짜'

            # [3. 인물 특성(트레이트) 및 능력치]
            '特质列表'                 = '특성 목록'
            '教育特质'                 = '교육 특성'
            '性格特质'                 = '성격 특성'
            '生活方式特质'             = '인생관 특성'
            '先天特质'                 = '천성 특성'
            '健康特质'                 = '건강 특성'
            '特质名称'                 = '특성 이름'
            '添加特质'                 = '특성 추가'
            '删除特质'                 = '특성 삭제'
            '特质'                     = '특성'
            '能力'                     = '능력치'
            '外交'                     = '외교'
            '军事'                     = '무력'
            '管理'                     = '관리'
            '谋略'                     = '지략'
            '学识'                     = '학습'
            '勇武'                     = '기량'

            # [4. 작위/영지 관련 잔여어]
            '领地'                     = '영지'
            '头衔'                     = '작위'
            '男爵领'                   = '남작령'
            '男爵'                     = '남작'
            '伯爵'                     = '백작'
            '公爵'                     = '공작'
            '国王'                     = '국왕'
            '皇帝'                     = '황제'
            '首都'                     = '수도'
            '颜色'                     = '색상'
            '法理'                     = '데 유레'
            '键值后缀不能为空'         = '접미사 키 값은 비어 있을 수 없습니다'
            '键值已存在'               = '키 값이 이미 존재합니다'
            '该键值已存在'             = '해당 키 값이 이미 존재합니다'
            '目标领地不能与原领地相同' = '대상 작위는 원래 작위와 같을 수 없습니다'
            '请选择目标领地'           = '대상 작위를 선택하세요'

            # [5. 파일 탐색 및 공통 버튼]
            '浏览...'                  = '찾아보기...'
            '浏览'                     = '찾아보기'
            '选择文件'                 = '파일 선택'
            '选择文件夹'               = '폴더 선택'
            '选择目录'                 = '디렉터리 선택'
            '清除'                     = '지우기'
            '重置'                     = '초기화'
            '全选'                     = '전체 선택'
            '反选'                     = '선택 반전'
            '刷新'                     = '새로고침'
            '加载'                     = '불러오기'
            '导入'                     = '가져오기'
            '导出'                     = '내보내기'
            '搜索'                     = '검색'

            # [6. 속성창 및 개인정보]
            '个人信息'                 = '개인 정보'
            '基本信息'                 = '기본 정보'
            '性别'                     = '성별'
            '男性'                     = '남성'
            '女性'                     = '여성'
            '宗教名'                   = '종교명'
            '文化名'                   = '문화명'
            '가문名'                   = '가문명'
            '中文家族名'               = '한글가문명'
            '中文가문名'               = '한글가문명'
            '英文家族名'               = '영문가문명'
            '英文가문名'               = '영문가문명'
            '属性设置窗口'             = '속성 설정 창'
            '属性设置'                 = '속성 설정'
            '姓名'                     = '성명'
            '文化'                     = '문화'
            '宗教'                     = '종교'
            '父亲'                     = '아버지'
            '母亲'                     = '어머니'
            '配偶'                     = '배우자'
            '배우자'                   = '배우자'
            '搜索人物...'              = '인물 검색...'
            '选中人物的事件列表'       = '선택된 인물의 이벤트 목록'
            '额外信息'                 = '추가 정보'
            '增加事件'                 = '이벤트 추가'
            '删除当前选中事件'         = '현재 선택된 이벤트 삭제'

            # [7. 시스템/로그/다이얼로그]
            '加载가문数据失败'         = '가문 데이터 로드 실패'
            '保存가문数据失败'         = '가문 데이터 저장 실패'
            '获取가문'                 = '가문 조회: '
            '数据失败'                 = '데이터 처리 실패'
            '가문 추가失败'             = '가문 추가 실패'
            '更新가문数据失败'         = '가문 데이터 갱신 실패'
            '가문 삭제失败'             = '가문 삭제 실패'
            '데이터베이스初始化失败'   = '데이터베이스 초기화 실패'
            '데이터베이스迁移失败'     = '데이터베이스 마이그레이션 실패'
            '通用增删改查操作'         = '공통 CRUD 작업'
            '请先调用 Initialize 方法初始化 DebugHelper！' = '먼저 Initialize 메서드를 호출하여 DebugHelper를 초기화하세요!'
            '心跳文件不存在'           = '하트비트 파일이 존재하지 않음'
            '心跳文件为空'             = '하트비트 파일이 비어 있음'
            '心跳检测: 上次心跳'       = '하트비트 확인: 마지막 하트비트'
            '心跳文件格式오류'         = '하트비트 파일 형식 오류'
            '心跳检测异常:'            = '하트비트 감지 예외:'
            '加载数据时出错:'          = '데이터 로드 중 오류 발생:'
            '加载文件设置时出错:'      = '파일 설정 로드 중 오류 발생:'
            '保存数据时出错:'          = '데이터 저장 중 오류 발생:'
            '运行中'                   = '실행 중'
            '已停止'                   = '중지됨'
            '仅统计有完整出生/死亡记录的人' = '출생/사망 기록이 모두 있는 인물만 집계'
            '解析名称文件失败，请检查文件格式或路径是否正确。' = '이름 파일 파싱 실패. 게임 설치 경로를 확인하세요.'
            '就绪'                     = '준비 완료'

            # [8. 창 제목, 툴팁, 상단 메뉴]
            '작者:Fred'                = '제작: Fred'
            '作者:Fred'                = '제작: Fred'
            '法理编辑器'               = '영지 작위 에디터'
            '转换到纹章收集软件'       = '문장 수집 프로그램으로 전환'
            '显示数据库'               = '데이터베이스 화면으로 전환'
            '显示统计数据'             = '통계 화면으로 전환'
            '显示领地定义'             = '영지 작위 화면으로 전환'
            '显示时间轴'               = '타임라인 화면으로 전환'
            '保存所有文件内容'         = '모든 파일 내용 저장'
            '저장所有파일내용'         = '모든 파일 내용 저장'
            '转换到文件设置界面'       = '파일 설정 화면으로 전환'
            '转换到파일설정界面'       = '파일 설정 화면으로 전환'
            '转换到世系图界面'         = '가문/인물 편집 화면으로 전환'
            '转换到数据库界面'         = '데이터베이스 화면으로 전환'
            '转换到统计界面'           = '통계 화면으로 전환'
            '转换到领地定义界面'       = '영지 작위 화면으로 전환'
            '转换到영지정의界面'       = '영지 작위 화면으로 전환'
            '转换到时间轴界面'         = '타임라인 화면으로 전환'
            '코드 생성个人内容'        = '인물 코드 생성'
            '코드 생성家族内容'        = '가문 코드 생성'
            '코드 생성整个内容'        = '전체 코드 생성'
            '내보내기个人内容'         = '인물 코드 내보내기'
            '내보내기家族内容'         = '가문 코드 내보내기'
            '내보내기整个内容'         = '전체 코드 내보내기'
            '生成个人内容'             = '인물 코드 생성'
            '生成家族内容'             = '가문 코드 생성'
            '生成整个内容'             = '전체 코드 생성'
            '输出个人内容'             = '인물 코드 내보내기'
            '输出家族内容'             = '가문 코드 내보내기'
            '输出整个内容'             = '전체 코드 내보내기'
            '个人内容'                 = '인물 내용'
            '家族内容'                 = '가문 내용'
            '整个内容'                 = '전체 내용'

            # [9. 우클릭 메뉴]
            '添加家族'                 = '가문 추가'
            '添加가문'                 = '가문 추가'
            '添加人物'                 = '인물 추가'
            '添加宗教'                 = '종교 추가'
            '添加文化'                 = '문화 추가'
            '删除家族'                 = '가문 삭제'
            '删除가문'                 = '가문 삭제'
            '删除人物'                 = '인물 삭제'

            # [10. 메인 속성 및 공통]
            '实时个人内容'             = '실시간 인물 내용'
            '实时文件内容'             = '실시간 파일 내용'
            '实时纹章内容'             = '실시간 문장 내용'
            '家族'                     = '가문'
            '家族统计数据'             = '가문 통계 데이터'
            '杂项统计数据'             = '기타 통계 데이터'
            '人数'                     = '인원수'
            '时间轴'                   = '타임라인'
            '世系图'                   = '가계도'
            '数据库'                   = '데이터베이스'
            '连接'                     = '연결'
            '断开'                     = '연결해제'
            '配置'                     = '환경설정'
            '确定'                     = '확인'
            '取消'                     = '취소'
            '生成'                     = '코드 생성'
            '输出'                     = '내보내기'
            '编辑'                     = '편집'
            '错误'                     = '오류'
            '提示'                     = '알림'
          }

          $sortedKeys = $dict.Keys | Sort-Object -Property Length -Descending
          $targetFiles = Get-ChildItem -Path "CK3ModStudio" -Recurse -File | Where-Object { $_.Extension -match '\.(xaml|cs|resx)$' }

          foreach ($file in $targetFiles) {
            $content = [System.IO.File]::ReadAllText($file.FullName, [System.Text.Encoding]::UTF8)
            $content = $content.TrimStart([char]0xFEFF)
            $changed = $false

            # 혼합형 알림 메시지 정규식 치환
            $regexPatterns = @(
              '请[^\"]*?(?:선택|选择)[^\"]*?(?:가문|家族)[^\"]*',
              '请先선택[^\"]*?가문',
              '请先选择[^\"]*?家族',
              '请先선택[^\"]*',
              '请先选择[^\"]*'
            )
            foreach ($pat in $regexPatterns) {
              if ($content -match $pat) {
                $content = [System.Text.RegularExpressions.Regex]::Replace($content, $pat, '먼저 가문을 선택하세요')
                $changed = $true
              }
            }

            foreach ($k in $sortedKeys) {
              if ($content.Contains($k)) {
                $content = $content.Replace($k, $dict[$k])
                $changed = $true
              }
            }

            # XAML Name 식별자 내 공백 제거 (MC3010 방지)
            if ($file.Extension -eq '.xaml') {
              $newContent = [System.Text.RegularExpressions.Regex]::Replace($content, '(?<=(?:x:)?Name=")[^"]+', {
                param($m)
                return $m.Value.Replace(' ', '_')
              })
              if ($newContent -ne $content) {
                $content = $newContent
                $changed = $true
              }
            }

            if ($changed) {
              [System.IO.File]::WriteAllText($file.FullName, $content, $utf8WithBom)
            }
          }

      - name: Audit and Report Remaining Chinese
        shell: pwsh
        run: |
          Write-Host "=== 잔여 중국어 전수 스캔 및 Summary 보고서 생성 ==="
          $chineseRegex = '[\u4e00-\u9fa5]'
          $remaining = @()

          Get-ChildItem -Path "CK3ModStudio" -Recurse -File | Where-Object { $_.Extension -match '\.(xaml|cs)$' } | ForEach-Object {
            $lines = [System.IO.File]::ReadAllLines($_.FullName, [System.Text.Encoding]::UTF8)
            for ($i = 0; $i -lt $lines.Count; $i++) {
              $raw = $lines[$i].Trim()
              if ($raw.StartsWith('//') -or $raw.StartsWith('#region') -or $raw.StartsWith('#endregion') -or $raw.StartsWith('/*') -or $raw.StartsWith('*')) {
                continue
              }
              $codeOnly = ($raw -replace '//.*$', '').Trim()
              if ($codeOnly -match $chineseRegex) {
                $remaining += "$($_.Name):L$($i+1) -> $codeOnly"
              }
            }
          }

          # GitHub Actions Summary 페이지에 마크다운 목록 직접 출력
          "### 🔍 소스 코드 내 잔여 중국어 목록 (총 $($remaining.Count)건)" | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Encoding utf8
          if ($remaining.Count -gt 0) {
            "| 파일:줄번호 | 남은 코드/문자열 |" | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Encoding utf8 -Append
            "| :--- | :--- |" | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Encoding utf8 -Append
            foreach ($item in $remaining) {
              $parts = $item -split ' -> '
              "| ``$($parts[0])`` | ``$($parts[1])`` |" | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Encoding utf8 -Append
            }
            Write-Host "::warning::잔여 중국어 $($remaining.Count)건 발견 (Summary 탭에 전체 표 생성됨)"
          } else {
            "🎉 모든 실행 코드 및 UI에서 중국어가 완전히 제거되었습니다!" | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Encoding utf8 -Append
            Write-Host "=== 잔여 중국어 0건 달성! ==="
          }

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v2

      - name: Restore NuGet packages
        run: nuget restore CK3ModStudio.sln

      - name: Build Solution
        run: msbuild CK3ModStudio.sln /p:Configuration=Release /v:minimal

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: CK3ModStudio-Korean-Fixed
          path: |
            **/bin/Release/
            **/bin/x64/Release/
